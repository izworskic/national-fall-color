import fs from 'node:fs';
import path from 'node:path';

const pagePath = path.join(process.cwd(), 'public/national-tools/fall-color/blue-ridge-parkway/index.html');
let html = fs.readFileSync(pagePath, 'utf8');

const leafletCss = '<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">';
if (!html.includes('leaflet@1.9.4/dist/leaflet.css')) html = html.replace('</head>', `${leafletCss}\n</head>`);

html = html.replace('<meta property="og:description" content="An elevation-aware Blue Ridge Parkway fall-color decision tool: best corridor now, next 7 days, weather stress, drought, observations and road context.">', '<meta property="og:description" content="Find the best Blue Ridge Parkway fall color near Asheville, Boone, Virginia and Cherokee, compare today with next week, check map pins, cameras and road status.">');
html = html.replace('<div class="persona-planner" data-persona-planner="true">', '<div id="trip-planner" class="persona-planner" data-persona-planner="true">');
html = html.replace('<span>evidence used</span>', '<span>what this call uses</span>');

const oldCompare = `<section class="section"><div class="wrap">
<h2>Compare the whole Parkway</h2>
<p class="section-intro">Use this when you are flexible enough to chase color. The sampling points show how timing changes with latitude and elevation from Virginia to the southern Blue Ridge.</p>
<div class="profile-card"><svg id="profile" viewBox="0 0 900 220" role="img" aria-labelledby="profile-title profile-desc"><title id="profile-title">Blue Ridge Parkway elevation profile sampling points</title><desc id="profile-desc">Representative mileposts plotted by elevation from northern Virginia to the southern Blue Ridge.</desc><line x1="50" y1="185" x2="870" y2="185" stroke="#d8d2c6"/><text x="50" y="208" class="profile-axis">MP 0 · Virginia</text><text x="810" y="208" class="profile-axis">MP 469 · NC</text><g id="profile-points"></g></svg></div>
<div id="corridor-list" class="corridor-list"><div class="corridor-card"><div class="skeleton">Loading corridor…</div></div></div>
</div></section>`;

const newCompare = `<section id="parkway-map-section" class="section"><div class="wrap">
<h2>Where the color is along the Parkway</h2>
<p class="section-intro">Tap a pin to compare places. The pin colors change when you switch between <strong>Today</strong> and <strong>7 days from now</strong> above, so you can see where the better color window is moving.</p>
<div class="map-status-row"><strong id="map-scope-label">Showing today · entire Parkway</strong><a href="#trip-planner">Change area or date ↑</a></div>
<div class="foliage-legend" aria-label="Fall color outlook legend">
  <span><i style="--legend:#2f6b3d"></i>Mostly green</span>
  <span><i style="--legend:#c3a82e"></i>Starting to turn</span>
  <span><i style="--legend:#e28a2d"></i>Good color likely</span>
  <span><i style="--legend:#cb4b2f"></i>Best color window</span>
  <span><i style="--legend:#9a5a3d"></i>Late color</span>
  <span><i style="--legend:#72685d"></i>Past prime</span>
</div>
<div class="parkway-map-shell"><div id="parkway-map" role="region" aria-label="Blue Ridge Parkway fall color outlook map"><div class="map-loading">Loading Parkway map…</div></div></div>
<p class="map-note"><strong>How to read this:</strong> Pin colors are the timing outlook for that place, not a live camera reading. The larger outlined pin is the best bet for the area and date you chose. Check a live camera before making a long drive.</p>
<h3 class="stop-list-title">Stops in this view</h3>
<div id="corridor-list" class="corridor-list"><div class="corridor-card"><div class="skeleton">Loading Parkway stops…</div></div></div>
</div></section>`;
if (!html.includes(oldCompare)) throw new Error('Compare Parkway section anchor missing');
html = html.replace(oldCompare, newCompare);

html = html.replace('<h2>More evidence behind the Parkway-wide model</h2>', '<h2>What could change the color</h2>');
html = html.replace('<p class="section-intro">Weather, drought and plant observations help explain why this year may look different from the historical timing. They stay separate from the simple trip call above.</p>', '<p class="section-intro">These are the things that can make the leaves look better or worse than the calendar suggests.</p>');
html = html.replace('<div class="context"><h3>Colored-leaf observations</h3>', '<div class="context"><h3>What people are seeing</h3>');
html = html.replace('<div class="context"><h3>7-day weather stress</h3>', '<div class="context"><h3>Weather that could knock leaves down</h3>');
html = html.replace('<div class="context"><h3>Drought / tree stress</h3>', '<div class="context"><h3>Dryness that can dull color</h3>');
html = html.replace('<h2>How this works, if you want the details</h2>', '<h2>How we make the call</h2>');
html = html.replace('<div class="method"><h3>1. Historical transition at each milepost</h3><p>USA-NPN MODIS Mid Green-down Median and historical variability are sampled independently at each Parkway anchor. This creates different timing windows instead of one regional date.</p></div>', '<div class="method"><h3>1. We compare each stop with its usual fall timing</h3><p>Every elevation turns on a different schedule. We compare each Parkway stop with the time of year it usually makes its strongest fall change instead of pretending the whole road peaks at once.</p></div>');
html = html.replace('<div class="method"><h3>2. Current plants are evidence, not a percentage</h3><p>Recent USA-NPN Nature’s Notebook “Colored leaves” observations near the selected anchor can raise confidence that autumn change is underway, but sparse volunteer records never become a landscape percentage.</p></div>', '<div class="method"><h3>2. We check current leaf reports</h3><p>Recent colored-leaf reports help tell us whether this season looks early, late or about normal. We never turn a few reports into a fake “percent peak.”</p></div>');
html = html.replace('<div class="method"><h3>3. Weather and drought explain quality risk</h3><p>NWS hard-freeze and wind signals plus the current U.S. Drought Monitor category are shown separately because leaf drop and stress color can make the woods look different from the historical timing curve.</p></div>', '<div class="method"><h3>3. We check weather and dryness</h3><p>Hard freezes, strong wind and dry conditions can shorten the show or dull the color, so we flag those separately from the timing outlook.</p></div>');
html = html.replace('<div class="method"><h3>4. Road closures can override the trip</h3><p>The tool checks the NPS road-status page and deprioritizes a selected anchor only when a closure range can be clearly parsed. No parsed closure does not mean the road is guaranteed open.</p></div>', '<div class="method"><h3>4. We check whether you can actually get there</h3><p>A great color window is useless if the Parkway is closed. We flag clearly reported NPS closures and still tell you to verify the official road page before leaving.</p></div>');
html = html.replace('<div class="callout"><strong>NPS context:</strong> The National Park Service notes that the Parkway runs nearly 470 miles and varies by more than 5,000 feet in elevation, so there is no single exact peak date. Higher elevations can begin changing in late September, with the broader progression typically moving through October. <a href="https://www.nps.gov/blri/learn/nature/fall-colors.htm">Read the NPS fall color guidance</a>.</div>', '<div class="callout"><strong>Why the map moves:</strong> The Parkway runs nearly 470 miles and climbs through huge elevation changes. Higher ridges usually turn first, then the better color tends to move lower as the season goes on. <a href="https://www.nps.gov/blri/learn/nature/fall-colors.htm">NPS fall color guidance</a>.</div>');
html = html.replace('<div class="method"><h3>When is peak color on the Blue Ridge Parkway?</h3><p>There is no single honest peak date for all 469 miles. The useful question is which corridor and elevation band is best matched to the transition now, then how that changes over the next week.</p></div>', '<div class="method"><h3>When is peak color on the Blue Ridge Parkway?</h3><p>There is not one peak day for all 469 miles. Use the map to see which part of the Parkway has the best color window now, then switch to 7 days from now to see where it may move next.</p></div>');
html = html.replace('<div class="method"><h3>Is this a live color camera or percent-peak map?</h3><p>No. It is a decision engine built from historical satellite phenology plus separately labeled current evidence. It will not invent a precision the public data cannot support.</p></div>', '<div class="method"><h3>Are the map pin colors live camera readings?</h3><p>No. They are a timing outlook for each Parkway stop. Use the live-camera section for the visual check before you drive. We do not invent a fake percent-peak number.</p></div>');
html = html.replace('<div class="method"><h3>What should I use for a weekend drive?</h3><p>Start with the current and +7-day corridor picks here, then verify NPS road status and local weather before committing to a specific milepost.</p></div>', '<div class="method"><h3>How should I use this for a weekend drive?</h3><p>Pick the area you can reach, compare Today with 7 days from now, tap the map pins, then check a live camera and NPS road status before you leave.</p></div>');

const mapCss = `.parkway-map-shell{border:1px solid var(--line);border-radius:7px;overflow:hidden;background:#e9eee8;margin-top:10px}.parkway-map-shell #parkway-map{height:520px;width:100%;font-family:Arial,sans-serif}.map-loading{padding:20px;color:#6c685f}.map-status-row{display:flex;justify-content:space-between;gap:14px;align-items:center;margin:12px 0 8px;font:13px/1.4 Arial,sans-serif}.map-status-row strong{color:var(--green2)}.map-status-row a{font-weight:700}.foliage-legend{display:flex;flex-wrap:wrap;gap:8px 14px;margin:8px 0 12px;font:12px/1.3 Arial,sans-serif;color:#5f5b53}.foliage-legend span{display:inline-flex;align-items:center;gap:6px}.foliage-legend i{width:12px;height:12px;border-radius:50%;background:var(--legend);border:1px solid rgba(0,0,0,.18);display:inline-block}.map-note{font:12.5px/1.5 Arial,sans-serif;color:#67635c;margin:10px 2px 20px}.stop-list-title{margin-top:6px;color:var(--green2)}.foliage-pin-wrap{background:transparent;border:0}.foliage-pin{width:26px;height:26px;background:var(--pin);border:2px solid #fff;border-radius:50% 50% 50% 0;transform:rotate(-45deg);box-shadow:0 2px 7px rgba(0,0,0,.35)}.foliage-pin.best{box-shadow:0 0 0 4px rgba(255,255,255,.95),0 0 0 7px #183f38,0 3px 9px rgba(0,0,0,.35)}.map-popup strong{display:block;font:700 16px/1.25 Georgia,serif;color:#26352a;margin-bottom:4px}.map-popup .map-stage{font:700 12px/1.25 Arial,sans-serif;margin-bottom:7px}.map-popup .map-meta{font:12px/1.45 Arial,sans-serif;color:#655f57}.map-popup .map-best{margin-top:8px;padding:6px 8px;background:#eef5ec;border-radius:3px;font:700 11px/1.3 Arial,sans-serif;color:#214a22}.map-popup .map-road{margin-top:7px;color:#8b3a31;font:700 11px/1.3 Arial,sans-serif}.map-popup a{display:inline-block;margin-top:8px;font:700 11px/1.3 Arial,sans-serif}.corridor-card.outside-focus{opacity:.45}.corridor-stage{display:inline-flex;align-items:center;gap:6px;font:700 12px/1.3 Arial,sans-serif}.corridor-stage i{width:10px;height:10px;border-radius:50%;background:var(--stage-color);display:inline-block}.leaflet-control-attribution{font-size:10px!important}@media(max-width:760px){.parkway-map-shell #parkway-map{height:460px}.map-status-row{display:block}.map-status-row a{display:inline-block;margin-top:4px}}@media(max-width:430px){.parkway-map-shell #parkway-map{height:420px}.foliage-legend{display:grid;grid-template-columns:1fr 1fr;gap:7px 10px}}`;
if (!html.includes('.parkway-map-shell{')) html = html.replace('</style>', `${mapCss}\n</style>`);

const oldProfileFn = / function renderProfile\(corridor,bestId\)\{[\s\S]*?\n \}\n function renderCorridor/;
if (!oldProfileFn.test(html)) throw new Error('renderProfile function anchor missing');
const mapFunctions = ` function foliageStage(station,offset=0){
  const base=Number(station?.timing?.days_from_midpoint);const d=base+Number(offset||0);
  if(!Number.isFinite(d))return{label:"Timing unavailable",color:"#8a857d"};
  if(d<-21)return{label:"Mostly green",color:"#2f6b3d"};
  if(d<-8)return{label:"Starting to turn",color:"#c3a82e"};
  if(d<-2)return{label:"Good color likely",color:"#e28a2d"};
  if(d<=7)return{label:"Best color window",color:"#cb4b2f"};
  if(d<=18)return{label:"Late color",color:"#9a5a3d"};
  return{label:"Past prime",color:"#72685d"};
 }
 let parkwayMap=null,parkwayMarkers=null;
 function ensureParkwayMap(){
  const el=$("parkway-map");if(!el)return null;
  if(!window.L){el.innerHTML='<div class="map-loading">Map tiles are unavailable right now. Use the stop-by-stop list below.</div>';return null}
  if(parkwayMap)return parkwayMap;
  el.innerHTML="";
  parkwayMap=L.map(el,{scrollWheelZoom:false,zoomControl:true});
  L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:15,attribution:'&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'}).addTo(parkwayMap);
  parkwayMarkers=L.layerGroup().addTo(parkwayMap);
  return parkwayMap;
 }
 function pinIcon(stage,isBest){
  return L.divIcon({className:'foliage-pin-wrap',html:'<div class="foliage-pin'+(isBest?' best':'')+'" style="--pin:'+stage.color+'"></div>',iconSize:[30,38],iconAnchor:[15,35],popupAnchor:[0,-31]});
 }
 function renderMap(corridor,bestId,offset=0,activeIds=null){
  const map=ensureParkwayMap();if(!map)return;
  parkwayMarkers.clearLayers();
  const active=Array.isArray(activeIds)&&activeIds.length?new Set(activeIds):null;
  const bounds=[];
  (corridor||[]).forEach(s=>{
    const lat=Number(s.latitude),lon=Number(s.longitude);if(!Number.isFinite(lat)||!Number.isFinite(lon))return;
    const stage=foliageStage(s,offset),isBest=s.id===bestId,isActive=!active||active.has(s.id);
    const marker=L.marker([lat,lon],{icon:pinIcon(stage,isBest),opacity:isActive?1:.32,title:s.name+': '+stage.label}).addTo(parkwayMarkers);
    const road=s.road?.possible_closure?'<div class="map-road">Possible NPS closure — check road status.</div>':'';
    const best=isBest?'<div class="map-best">Best bet for the area and date you selected</div>':'';
    marker.bindPopup('<div class="map-popup"><strong>'+esc(s.name)+'</strong><div class="map-stage" style="color:'+stage.color+'">'+esc(stage.label)+'</div><div class="map-meta">MP '+esc(s.milepost)+' · '+Number(s.elevation_ft||0).toLocaleString()+' ft<br>'+esc(s.corridor||s.state||'Blue Ridge Parkway')+'</div>'+best+road+'<a href="#live-cameras">Check live cameras ↓</a></div>');
    if(isActive)bounds.push([lat,lon]);
  });
  if(bounds.length){map.fitBounds(bounds,{padding:[32,32]});if(map.getZoom()>9)map.setZoom(9)}
  setTimeout(()=>map.invalidateSize(),0);
 }
 window.__renderBlueRidgeMap=renderMap;
 function renderCorridor`;
html = html.replace(oldProfileFn, mapFunctions);

html = html.replace(/function renderCorridor\(corridor,bestId\)\{[\s\S]*?\n \}\n function representativeDrive/, `function renderCorridor(corridor,bestId,offset=0,activeIds=null){
  const active=Array.isArray(activeIds)&&activeIds.length?new Set(activeIds):null;
  const shown=active?(corridor||[]).filter(s=>active.has(s.id)):(corridor||[]);
  $("corridor-list").innerHTML=shown.map(s=>{const stage=foliageStage(s,offset);return \`<article class="corridor-card" data-best="\${s.id===bestId}"><div><div class="mp">MP \${esc(s.milepost)} · \${esc(s.state)}</div><span class="pill \${s.id===bestId?'green':''}">\${s.id===bestId?'best bet':esc(s.corridor)}</span></div><div class="corridor-name"><strong>\${esc(s.name)}</strong><small>\${Number(s.elevation_ft).toLocaleString()} ft · \${esc(s.elevation_note)}</small></div><div class="cell"><div class="corridor-stage" style="--stage-color:\${stage.color}"><i></i>\${esc(stage.label)}</div><small>color outlook for selected date</small></div><div class="cell"><b>\${esc(windowText(s))}</b><small>usual color window</small>\${s.road?.possible_closure?'<span class="pill red">possible closure</span>':''}</div></article>\`}).join("")||'<div class="corridor-card">No Parkway stops are available for this view.</div>';
 }
 window.__renderBlueRidgeCorridor=renderCorridor;
 function representativeDrive`);

html = html.replace('renderProfile(data.corridor||[],best.id);renderCorridor(data.corridor||[],best.id);', 'renderMap(data.corridor||[],best.id,0,null);renderCorridor(data.corridor||[],best.id,0,null);');
html = html.replace('$("drive-segment").textContent=drive?"Suggested sampling corridor: MP "+drive.start+"–"+drive.end+" around "+best.name+". This is the Parkway stretch represented by the anchor, not a claim that color is uniform across every mile.":"Use the anchor milepost as the center of the recommendation and verify local conditions before a long drive.";', '$("drive-segment").textContent=drive?"Start around "+best.name+". If roads are open, explore roughly MP "+drive.start+"–"+drive.end+" so you can move up or down in elevation if the color is better nearby.":"Start around "+best.name+" and use the map plus live cameras before a long drive.";');
html = html.replace('$("updated").textContent=`Model retrieved ${new Date(data.retrieved_at).toLocaleString()} · Local corridor date ${data.local_date}${roadStamp}.`;', '$("updated").textContent=`Outlook updated ${new Date(data.retrieved_at).toLocaleString()} · Parkway date ${data.local_date}${roadStamp}.`;');
html = html.replace('$("why-timing").textContent=`Timing: historical mid-transition is ${fmtDate(best.historical_mid_transition)} with ${best.timing?.variability_mad_days==null?\'unknown\':(\'±\'+Math.round(best.timing.variability_mad_days)+\' days\')} historical MAD.`;', '$("why-timing").textContent=`Usual timing: this elevation most often makes its strongest fall change around ${fmtDate(best.historical_mid_transition)}.`;');
html = html.replace('$("why-current").textContent=obs?.available?`Regional plant observations: ${obs.interpretation} ${obs.yes_records||0} recent yes record${obs.yes_records===1?\'\':\'s\'} across ${obs.yes_sites||0} yes-reporting site${obs.yes_sites===1?\'\':\'s\'} within ${obs.radius_miles} miles. Elevation can differ sharply inside that radius, so these observations do not move the corridor ranking.`:"Regional plant observations: currently unavailable; no observation claim is substituted.";', '$("why-current").textContent=obs?.available?`Current leaf reports: ${obs.interpretation} We found ${obs.yes_records||0} recent colored-leaf report${obs.yes_records===1?\'\':\'s\'} nearby. Use the cameras below for the best visual check.`:"Current leaf reports are unavailable right now. Use the live cameras below before a long drive.";');
html = html.replace('$("why-quality").textContent=`Quality context: ${weather?.available?weather.quality_note:\'NWS weather context unavailable.\'} ${drought?.available?drought.interpretation:\'Drought context unavailable.\'}`;', '$("why-quality").textContent=`What could change the show: ${weather?.available?weather.quality_note:\'Weather detail is unavailable right now.\'} ${drought?.available?drought.interpretation:\'Dryness detail is unavailable right now.\'}`;');
html = html.replace('$("obs-context").textContent=obs?.available?`${obs.interpretation} Coverage is ${obs.coverage}; the latest record is ${obs.latest_observation_date?fmtDate(obs.latest_observation_date):\'not available\'}.`:"Current plant observations are unavailable right now. Historical timing remains separate and usable.";', '$("obs-context").textContent=obs?.available?`${obs.interpretation} Latest nearby report: ${obs.latest_observation_date?fmtDate(obs.latest_observation_date):\'date unavailable\'}.`:"Current leaf reports are unavailable right now.";');
html = html.replace('$("weather-context").textContent=weather?.available?`Forecast range ${weather.min_7d_f}–${weather.max_7d_f}°F; ${weather.hard_freeze_hours} hard-freeze hours, max modeled hourly wind ${weather.max_wind_mph??\'—\'} mph, max precipitation probability ${weather.max_precip_probability??\'—\'}%. ${weather.quality_note}`:"NWS context unavailable right now.";', '$("weather-context").textContent=weather?.available?`Next 7 days: ${weather.min_7d_f}–${weather.max_7d_f}°F, winds up to ${weather.max_wind_mph??\'—\'} mph and rain chances up to ${weather.max_precip_probability??\'—\'}%. ${weather.quality_note}`:"Weather detail is unavailable right now.";');
html = html.replace('$("drought-context").textContent=drought?.available?`${drought.category}. ${drought.interpretation}`:"U.S. Drought Monitor context unavailable right now.";', '$("drought-context").textContent=drought?.available?`${drought.interpretation}`:"Dryness detail is unavailable right now.";');

html = html.replace('function stageFor(diff){if(diff<-21)return "well before the usual transition";if(diff<-8)return "approaching the usual transition";if(diff<=7)return "inside the usual transition window";if(diff<=18)return "late in the usual transition window";return "typically past the main transition"}', 'function stageFor(diff){if(diff<-21)return "mostly green";if(diff<-8)return "starting to turn";if(diff<-2)return "good color likely";if(diff<=7)return "best color window";if(diff<=18)return "late color";return "past prime"}');
html = html.replace('function depth(best,offset){const original=offset?data?.decision?.best_next_7d:data?.decision?.best_now;return original?.id===best?.id?"Timing + conditions":"Timing + road"}', 'function depth(best,offset){const original=offset?data?.decision?.best_next_7d:data?.decision?.best_now;return original?.id===best?.id?"Timing + live checks":"Timing + road check"}');
html = html.replace('function timingText(best,offset){const base=Number(best?.timing?.days_from_midpoint);if(!Number.isFinite(base))return "Historical timing is limited for this point.";const diff=base+offset;if(Math.abs(diff)<=2)return "This date is very close to the location’s usual mid-transition.";return diff<0?"This date is about "+Math.abs(Math.round(diff))+" days before the location’s usual mid-transition.":"This date is about "+Math.round(diff)+" days after the location’s usual mid-transition."}', 'function timingText(best,offset){const base=Number(best?.timing?.days_from_midpoint);if(!Number.isFinite(base))return "We do not have enough usual-timing history for this stop.";const diff=base+offset;if(Math.abs(diff)<=2)return "This date lines up closely with when this elevation usually makes its strongest fall change.";return diff<0?"This date is about "+Math.abs(Math.round(diff))+" days before color usually gets strongest at this elevation.":"This date is about "+Math.round(diff)+" days after this elevation’s usual strongest fall-change window."}');
html = html.replace('if(diff<-21)return{level:"early",label:"WAIT",headline:"Too early for a foliage-only trip",detail:best.name+" is the strongest option near "+labels[focus]+", but "+when+" it is still about "+Math.abs(Math.round(diff))+" days before its usual mid-transition."};if(diff<-8)return{level:"watch",label:"WATCH",headline:"Color season is getting closer",detail:best.name+" is the best timing match near "+labels[focus]+" "+when+", but it is still about "+Math.abs(Math.round(diff))+" days before its usual midpoint."};if(diff<=7)return{level:"go",label:"GO",headline:"This is a strong historical color window",detail:best.name+" is inside its usual transition window "+when+" and is the strongest timing match among the Parkway points in this area."};if(diff<=18)return{level:"soon",label:"GO SOON",headline:"Still worth a look, but do not wait too long",detail:best.name+" is on the late side of its usual transition window "+when+". Wind and leaf drop matter more now."};return{level:"late",label:"LATE",headline:"The best remaining option is past its usual prime window",detail:best.name+" is still the strongest timing match near "+labels[focus]+", but lower elevations may offer better remaining color."}', 'if(diff<-21)return{level:"early",label:"WAIT",headline:"Mostly green — give it more time",detail:best.name+" is the best bet near "+labels[focus]+", but "+when+" this elevation is usually still well ahead of its stronger color window."};if(diff<-8)return{level:"watch",label:"WATCH",headline:"Color is starting to get closer",detail:best.name+" is the best bet near "+labels[focus]+" "+when+", but the stronger color window is usually still about "+Math.abs(Math.round(diff))+" days away."};if(diff<=7)return{level:"go",label:"GO",headline:"Good time to chase color",detail:best.name+" lines up with the time of year this elevation usually shows its strongest fall change. Check the cameras before a long drive."};if(diff<=18)return{level:"soon",label:"GO SOON",headline:"Still worth a look — do not wait too long",detail:best.name+" is on the late side now. Wind and leaf drop could shorten the remaining color window."};return{level:"late",label:"LATE",headline:"Higher-elevation color is likely fading",detail:best.name+" is still the best bet near "+labels[focus]+", but lower elevations may have better remaining color."}');

const personaDispatch = 'const derived={...data,__personaDerived:true,decision:{...data.decision,best_now:best}};window.__blueRidgeDecision=derived;window.dispatchEvent(new CustomEvent("blue-ridge-decision-ready",{detail:derived}))';
if (!html.includes(personaDispatch)) throw new Error('persona dispatch anchor missing');
html = html.replace(personaDispatch, 'const ids=groups[focus];if(window.__renderBlueRidgeMap)window.__renderBlueRidgeMap(data.corridor||[],best.id,horizon,ids);if(window.__renderBlueRidgeCorridor)window.__renderBlueRidgeCorridor(data.corridor||[],best.id,horizon,ids);const mapLabel=$("map-scope-label");if(mapLabel)mapLabel.textContent="Showing "+(horizon?"7 days from now":"today")+" · "+labels[focus];const derived={...data,__personaDerived:true,decision:{...data.decision,best_now:best}};window.__blueRidgeDecision=derived;window.dispatchEvent(new CustomEvent("blue-ridge-decision-ready",{detail:derived}))');

html = html.replace('if(!evidence?.available||!signal?.available)return "Automated camera evidence is unavailable, so it does not change the timing recommendation.";', 'if(!evidence?.available||!signal?.available)return "We could not get the research-camera data right now. Use the live-view links below instead.";');
html = html.replace('if(distance!=null&&distance>60)return "The machine-analyzed PhenoCam is about "+Math.round(distance)+" Parkway mileposts from the current pick. Treat it as regional context, not evidence that overrides this drive.";', 'if(distance!=null&&distance>60)return "The research camera is about "+Math.round(distance)+" Parkway mileposts from this drive, so use the closer live-view links below for the visual check.";');
html = html.replace('if(visuallyEarly&&Number.isFinite(d)&&d>=-8)return "Camera greenness near this part of the Parkway is running greener than historical timing alone would suggest. That is a reason to temper the GO signal, not to invent a new peak date.";', 'if(visuallyEarly&&Number.isFinite(d)&&d>=-8)return "The nearby research camera still looks greener than the calendar suggests. Check the live views before making a long drive.";');
html = html.replace('if(visuallyActive&&Number.isFinite(d)&&d<-8)return "Camera greenness suggests autumn transition is arriving earlier near this part of the Parkway than historical timing alone would imply.";', 'if(visuallyActive&&Number.isFinite(d)&&d<-8)return "The research camera is showing fall change a little earlier than usual for this part of the Parkway.";');
html = html.replace('return "Camera greenness and historical timing are broadly compatible near this part of the Parkway. The camera remains supporting evidence, not a percent-peak measurement.";', 'return "The research camera broadly agrees with the fall-color timing for this part of the Parkway. Use the live views below for the final look.";');
html = html.replace('const mode=cam.machine_analysis==="phenocam-gcc"?"Derived GCC analyzed here; image stays at source":"Open source camera/view";', 'const mode=cam.machine_analysis==="phenocam-gcc"?"Research camera data used in this outlook":"Open live view";');
html = html.replace('$("visual-stage").textContent=signal.stage+" · "+signal.direction;', 'const friendly=/summer-like/i.test(signal.stage)?"Mostly green":/early transition/i.test(signal.stage)?"Starting to turn":/transition underway/i.test(signal.stage)?"Color building":/advanced transition/i.test(signal.stage)?"Good color":/late transition/i.test(signal.stage)?"Late color":/leaf-off/i.test(signal.stage)?"Past peak":signal.stage;$("visual-stage").textContent=friendly;');
html = html.replace('$("visual-updated").textContent="Visual evidence retrieved "+new Date(visual.retrieved_at).toLocaleString()+" · "+(visual.camera_registry?.machine_analyzable||0)+" machine-analyzable camera source · "+(visual.camera_registry?.total||0)+" corridor camera links registered.";', '$("visual-updated").textContent="Camera check updated "+new Date(visual.retrieved_at).toLocaleString()+" · "+(visual.camera_registry?.total||0)+" Parkway-area camera links available.";');
html = html.replace('$("visual-reconcile").textContent="The core timing model is still available. No camera or satellite conclusion is substituted while visual verification is unavailable.";', '$("visual-reconcile").textContent="The fall-color outlook is still available. Use the camera links below or check back for the visual layer.";');
html = html.replace('<span>change from summer green</span>', '<span>change from summer</span>');
html = html.replace('<span>7-day canopy trend</span>', '<span>change this week</span>');
html = html.replace('<summary>Satellite coverage and technical cross-check</summary>', '<summary>Technical: satellite coverage</summary>');

if (!html.includes('leaflet@1.9.4/dist/leaflet.js')) html = html.replace('<script>\n(function(){', '<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>\n<script>\n(function(){');

fs.writeFileSync(pagePath, html);
console.log('Upgraded Blue Ridge to a real geographic fall-color map with dynamic pins and visitor-first language.');
